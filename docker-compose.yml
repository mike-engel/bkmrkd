version: '2'
services:
  postgres:
    image: postgres:9.6
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=bkmrkd_development
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=postgres
